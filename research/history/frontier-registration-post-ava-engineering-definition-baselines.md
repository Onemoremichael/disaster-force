# Post-AVA frontier registration: Engineering Definition Baselines

**Registration:** `FR-03`  
**Status:** one question opened before Pass 236  
**Branch:** `EDB`, zero of at most five passes consumed

## Stocktake

Pass 235 returned a complete authority architecture for turning funded design work into reconstructable, payable artifacts. It did not create the artifacts. The six cohort-one forms still exist as interval-bounded hypotheses with paper-closed internal sections, specified destructive events and explicit missing physical coefficients:

- `ET-A-A`: twin-body pelagic exchange;
- `CT-C-B`: anchored occupied-boundary utility belt;
- `HC-A-C`: distributed rotorborne atmospheric-intervention cells;
- `LF-B-A`: radial multi-bore geothermal water–power source;
- `HU-A-B`: bypassed-spine clinical continuity island; and
- `RT-B-C`: parallel mobile regeneration trainlets.

The project has enough constitutional machinery. Its next consequential deficit is engineering definition: controlled load cases, conserved-account closure, source provenance, uncertainty, fatal margins, invention dependencies and reproducible build-to-test content for all six forms.

## Resolution test

If this branch's answer were reversed, cohort one would either remain a portfolio of broad planning envelopes or move to physical construction on contractor-defined assumptions; either result changes the first physical portfolio, its technology program and the Department's acquisition architecture. `EDB` therefore passes the project-resolution test.

## Registered branch question

Can the six cohort-one forms be converted from interval-bounded, paper-balanced hypotheses into six source-specific **Engineering Definition Baselines** that close service, mass, energy, flow, thermal, geometry, civil-rights, residual and cold-reconstruction accounts under nominal, largest-cell-loss, fatal-keystone and cold-start worlds; expose the inventions and measured coefficients that dominate each; and produce reconstructable build-to-test packages without converting design closure into evidence, `MAKE`, `REP-4` or form selection?

## First open item

What configuration grammar, load-case set, source-provenance law and noncompensatory closure rule can define all six baselines without importing a common machine, hiding unknowns or treating interval midpoints as measurements?

Pass 236 must close this item. It may open at most one successor, so the running total remains one or falls to zero.

## Five-pass budget

1. `EDB-1`: configuration grammar, load cases, provenance and closure law.
2. `EDB-2`: numerical service, mass, energy, flow and thermal closure with uncertainty.
3. `EDB-3`: geometry, structural, control, rights, residual and safe-state closure.
4. `EDB-4`: invention-critical build-to-test articles, industrial paths and cost exposure.
5. `EDB-5`: terminal design-baseline compact, rendering-eligibility judgment and return.

This sequence is a budget, not a presumption that every branch survives. A fatal common architecture, irreducible missing account or source-provenance failure may require `KILL` before the fifth pass. No rendering is authorized by registration.

## Registration ledger

Items opened: 1 — the `EDB-1` configuration, load-case, provenance and closure question.  
Items closed: 0 — this is a pre-pass registration, not a numbered pass.  
Running open total: 1.  
No-`KILL` count: unchanged at five since `HR-02`.
